[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "quantfinance-course"
version = "0.1.0"
description = "Computational quantitative finance course in Python"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
